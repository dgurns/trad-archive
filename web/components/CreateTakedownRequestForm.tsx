import { Entity } from "types";

interface Props {
	entity: Entity;
}

const CreateTakedownRequestForm = ({ entity }: Props) => {
	return <div>Form goes here</div>;
};

export default CreateTakedownRequestForm;
